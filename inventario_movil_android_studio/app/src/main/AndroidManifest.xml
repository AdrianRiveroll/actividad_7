<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:label="InventarioMovil"
        android:supportsRtl="true"
        android:theme="@style/Theme.InventarioMovil"
        android:networkSecurityConfig="@xml/network_security_config">



    <activity android:name=".ui.sales.SaleDetailActivity" />
        <activity android:name=".ui.admin.AdminSalesActivity" />
        <activity android:name=".ui.admin.AdminProductsActivity" />
        <activity android:name=".ui.admin.AdminDashboardActivity" />
        <activity android:name=".ui.client.CartActivity" />
        <activity android:name=".ui.client.ClientProductsActivity" />

        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
